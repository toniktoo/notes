import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div``

type PropType = {};

export const Timer: React.FC<PropType> = () => {
    return (
        <Wrapper>

        </Wrapper>
    );
};